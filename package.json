{
  "name": "Parcel-Pug-SASS",
  "version": "1.0.0",
  "main": "src/views/home.pug",
  "license": "MIT",
  "scripts": {
    "start": "yarn cache clean && rm -rf dist && parcel src/views/home.pug --open",
    "build:parcel": "parcel build src/views/home.pug -d build --public-url ./ --out-file index.html",
    "build": "yarn cache clean && rm -rf build && yarn build:parcel"
  },
  "dependencies": {
    "@babel/preset-react": "^7.7.4",
    "autoprefixer": "^9.7.3",
    "parsel": "^0.3.0",
    "postcss-modules": "^1.4.1"
  },
  "devDependencies": {
    "@babel/core": "^7.7.7",
    "cssnano": "^4.1.10",
    "pug": "^2.0.4",
    "sass": "^1.24.0"
  }
}
